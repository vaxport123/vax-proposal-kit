# 바뀐 것

판 번호는 `VERSION`에 있습니다. 새 판을 낼 때는 사람이 `git tag v0.6.0`처럼 태그를 답니다.
`doctor.sh`가 이 판과 원격 태그를 비교해 새 판이 있으면 알려 줍니다.
회사 사실·사업명·공고번호·금액은 여기에 적지 않고, 「대학 캠퍼스 VR 건」처럼 일반화해 씁니다.

## 0.6.2 (2026-09-06)

- 자료가 얇으면 요구하고 끝나면 결과를 묻습니다. `references/asking.md`(묻기 원칙 여섯·단계별 결손 카탈로그·회고 일곱·배운 것을 두 갈래로 두는 법)와 `scripts/gap_check.py`(사업 폴더를 읽어 빈 절·출처 없는 사실·카드 부족·재료점검 미달·승인 없음·회고 없음을 세어 질문서 초안 생성). SKILL P0·P3.5·P4·P6·P7에 묻는 지점, 원칙 8, `_STATE.md` 회고 칸.

## 0.6.1 (2026-09-06)

- 준비 점검을 사람 손에서 Claude로 옮겼습니다. 스킬 첫 동작 P-1이 `scripts/preflight.py`를 돌려 필수·권장을 판정하고 커넥터(피그마 계정·라이브러리 접근·노션 재료 팩)를 도구로 확인해, 빠진 것을 한 번에 요구합니다. 필수가 남으면 P0로 가지 않습니다.

## 0.6.0 (2026-09-06)

- 제3자(직원)가 반복해서 쓰도록 설치·운영을 손봤습니다. 첫 실행 안내 `START.md`, 판 관리(`VERSION`·이 파일), 커밋 훅, 문의 창구, 윈도우 콘솔 한글 방어를 넣었습니다.
- 발주처·과업을 웹에서 자동으로 긁어 오는 `probe_web.py`를 넣었습니다. 검색어를 출처 유형 일곱으로 나눠 무료 검색으로 돌리고, 페이지를 열어 숫자가 든 문장을 뽑고, 기관 유형별 공식 데이터 바로가기와 발상 자극 카드를 만들어 줍니다.
- 아이디어를 뽑는 「색채」 절차를 넣었습니다. 발상 기법 일곱과 컨셉 이름 짓기, 그리고 「담당자가 한 문장으로 옮길 수 있나」라는 거르기 기준을 더했습니다.
- 회사 손 덱 세 개(118장)에서 화면 문법과 틀 열네 종을 재어 `layouts.json` 하나로 정본화했습니다. 와이어프레임 그림과 Figma 좌표를 여기서 함께 만듭니다.

## 0.5.0 (2026-09-06)

- 슬라이드 만드는 방식을 뒤집었습니다. 문단을 슬라이드에 흘려 넣던 방식을 지우고, 재료 게이트 → 고스트 덱(장마다 주장 제목 하나 + 증거 하나) → 사람 승인 → Figma 순서로 바꿨습니다. 대학 캠퍼스 VR 건을 세 번 고쳐도 문서 같은 덱이 나온 원인이 방식 자체였기 때문입니다.
- 발표에서 그대로 말할 수 있는 문장 규칙을 `writing.md`에 넣었습니다. 문서체와 말하는 문장을 열 쌍으로 대조하고, 소리 내어 읽어 어색하면 고치도록 했습니다.

## 0.4.0 (2026-09-05)

- 그림 레시피 열 종(동선 지도·연결도·배치도·공정도·층도·스토리보드 등)을 손으로 그리지 않고 함수로 만들도록 옮겼습니다.
- 말투를 손봤습니다. 압축체를 막고(40자 상한은 제목에만), 내부에서만 쓰는 조어가 화면에 새어 나가지 않게 하고, 결론 문장이 같은 틀로 반복되지 않게 했습니다.
- 규칙을 게이트가 아니라 경향으로 낮췄습니다. 규칙을 하드 게이트로 걸면 덱이 글 문서가 되기 때문입니다. 게이트는 RFP와 관련된 것만 남기고 나머지는 경향으로 조절합니다.
- 규칙 파일 열두 개를 글(writing)·덱(deck)·채점(review-rubric) 셋으로 줄였습니다. 46장 덱이 한 얼굴이 된 원인을 없앴습니다.
- 반복 사용을 위한 장치 셋을 넣었습니다. 정규식으로 글·계획을 검사하는 `proposal_lint.py`, Figma 공식 배치 검증(validate), 이름을 가린 완성 예시 한 세트입니다.
- 후보 스킬 보안 검토를 마쳐 몇 개를 채택하고 위험한 것은 제외했습니다.

## 0.3.0 (2026-09-04)

- 글 대신 SVG로 도식·표를 그리는 `figures_svg.py`(열네 종)와 `render_html.py`의 fig 블록을 넣었습니다.
- 웹에서 찾은 이미지를 출처와 함께 내려받는 `img_fetch.py`와 절차 문서를 넣었습니다.
- 비판자 에이전트 `proposal-critic`을 넣어 초안·덱의 AI 티와 디자인 문제를 잡게 했습니다.
- 제안 슬라이드 세 덱을 실측해 화면 문법(발주처 CI 강조색·Freesentation·주장형 제목·결론 띠·슬라이드 유형 열 종)을 정리했습니다.

## 0.2.0 (2026-09-04)

- 글꼴 Freesentation·Paperlogy(각 아홉 굵기, SIL OFL)와 자동 설치기를 넣었습니다. 사용자 글꼴 폴더에 깔아 관리자 권한이 필요 없습니다.
- 대학 캠퍼스 VR 건을 실제로 돌려 보고 드러난 결함 넷(배점 단위·기술 대 가격 어순·원문 미첨부·설치 스크립트 변수)을 고쳤습니다.

## 0.1.0 (2026-09-04)

- 첫 판. 제안 하네스 스킬(`vax-proposal`), 재료 팩 생성기(`proposal_material.py`, 서버용), `/bid-loop` 하네스, 설치 매니페스트(`skills.tsv`)와 설치·점검 스크립트를 넣었습니다.
- 회사 사실·레퍼런스 색인·인력 데이터는 저장소에 두지 않고 위키를 정본으로 삼도록 정리했습니다.
