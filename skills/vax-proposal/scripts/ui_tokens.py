#!/usr/bin/env python3
# ⚠️ 이 파일은 서버 `ops/ui_tokens.py`의 **사본**이다. 색·간격을 고칠 일이 있으면 서버 정본을 고치고
#    `server/sync_tokens.sh`로 여기로 옮긴다(반대로 하지 않는다). 2026-09-04 동기화.
"""웹 ERP 화면의 **기준값 한 곳** — 색·글자 크기·간격·손가락 최소 크기.

한준 2026-08-05 "기준 파일 + 화면 전체 재정비".

■ 왜 파일로 뺐나
기준이 `erp_web.py` 안의 200KB 문자열에 흩어져 있었다. 그래서 **같은 것이 화면마다 달라지는
일**이 반복됐다 : 팀 순서가 두 곳에 따로 있었고, 카드 제목이 표 머리와 같은 크기였고,
선택 표시 방식이 화면마다 달랐다. 한 곳에 모으면 그 종류의 문제가 구조적으로 막힌다.

■ 이 파일의 값은 **검사를 통과해야 한다**
`--selftest`가 글자색과 배경색의 대비를 **직접 계산**해서 기준(WCAG AA 4.5)에 못 미치면
실패한다. 색을 예쁘게 고치다 읽기 어려워지는 일을 사람 눈에 맡기지 않는다.
실측 2026-08-05 : `--mid`가 #737373이라 캔버스 위에서 4.35였다(14화면 전부 미달).
#6b6b6b로 올려 4.89가 됐다. 그 검사가 이 파일에 박혀 있으므로 다시 내려가지 않는다.

■ 무채색 원칙은 그대로다
색을 늘리지 않는다. 표면 단계(캔버스 → 카드 → 보조면)와 잉크 단계(잉크 → 연잉크 → 회색)뿐이고,
**빨강은 오류에만** 쓴다. 다크는 색을 새로 만들지 않고 표면 단계를 뒤집는다.
"""
import sys

# ── 표면과 잉크 ──────────────────────────────────────────────────────────────
# `surface`(배경) 위에 `ink`(글자)를 올린다. 아래 검사가 **모든 짝**의 대비를 재므로,
# 값을 고치면 못 미치는 짝이 바로 이름과 숫자로 드러난다.
LIGHT = {
    "canvas": "#f5f5f5",       # 화면 바닥
    "paper": "#ffffff",        # 카드
    "surface-alt": "#fafafa",  # 카드 안 보조면
    "ink": "#0a0a0a",          # 본문·제목
    "ink-soft": "#171717",     # 한 단 연한 본문
    "mid": "#6b6b6b",          # 흐린 글자(캡션·표 머리) — 2026-08-05에 #737373에서 올렸다
    "hairline": "#e5e5e5",     # 실선. **글자색으로 쓰지 않는다**(대비 1.16)
    "brand": "#082567",        # 로고 남색 — 링크·워드마크(회사 사이트에서 그대로 가져왔다)
    "brand-bg": "#e7ecf7",     # 옅은 바탕(브랜드 강조 배지)
    # 주 버튼은 **동작색**이 칠한다(아래 `action`) — 로고 남색은 회사를 가리키는 색으로
    # 남기고, 「누르면 무슨 일이 일어난다」는 한 색으로만 말한다.
    "action": "#663af3",
    "action-fg": "#ffffff",
    "ember": "#c60009",        # 오류 전용 — 2026-08-05에 #e7000b에서 낮췄다(캔버스 위 4.5 미달)
    "good": "#0f7038",         # 잘됨(정상·완료·승인·확정)
    "good-bg": "#e7f3ea",      # 그 배지의 옅은 바탕
    "warn": "#8f5000",         # 주의(지각·대기·마감 임박)
    "warn-bg": "#fbefda",
    # 구분용 셋(뜻 없음) — 캘린더처럼 **여럿을 갈라 보여야** 할 때. 위 뜻이 있는 색들과
    # 겹치지 않는 갈래를 골랐다(남색·빨강·초록·주황을 피해 청록 → 보라 → 자홍).
    "cat1": "#0b6b72", "cat1-bg": "#e2f2f3",
    "cat2": "#5b3ec8", "cat2-bg": "#ece7fb",
    "cat3": "#a5246b", "cat3-bg": "#fbe7f1",
    "fill-bg": "#0a0a0a",      # 채움(주 버튼·선택된 것)
    "fill-fg": "#fafafa",
    "fill-bg-2": "#171717",    # 진한 배지
}
DARK = {
    # ── 자정 유리(midnight glass) — 한준 2026-08-09 승인 ────────────────────────────
    # 다크를 **라이트의 반전**이 아니라 제 성격을 가진 판으로 다시 짰다. 바탕은 거의 검은
    # 남색이고, 카드는 그 위에 얹은 **서리 낀 유리판**(푸른 흰빛을 아주 옅게 섞은 면)이다.
    # 표면 세 단은 섞는 세기로만 갈린다 : 바닥 0% → 카드 안 판 3% → 카드 6%.
    # 값은 `#bad6f7`을 그 비율로 바탕에 섞어 미리 계산해 박아 둔 것이다(투명도를 쓰면
    # 대비를 계산할 수 없다 — 아래 검사가 실제 색으로 재야 한다).
    "canvas": "#05060f",       # 화면 바닥
    "paper": "#101422",        # 카드 = 서리 유리판
    "surface-alt": "#0b0e19",  # 카드 안 보조면(한 단 가라앉은 판)
    # 잉크는 **밝은 쪽에서 아래로** 내려온다 : 제목 → 본문 → 흐린 글자.
    "ink": "#d8ecf8",          # 본문·제목
    "ink-soft": "#d1e4fa",     # 한 단 연한 본문
    "mid": "#9da7ba",          # 흐린 글자(캡션·표 머리)
    # 실선은 **푸른 흰빛을 12%로 섞은 한 줄**이다 — 진한 회색 선을 긋지 않는다(유리 느낌은
    # 딱딱한 테두리가 아니라 빛나는 모서리에서 온다). **글자색으로 쓰지 않는다**(대비 1.43).
    "hairline": "#252b3a",
    # 로고 남색 #082567은 어두운 바탕에서 안 읽힌다 — 링크·워드마크는 이 푸른빛을 쓴다.
    "brand": "#b6d9fc",
    "brand-bg": "#1c1f29",     # 옅은 바탕(브랜드 강조 배지)
    "ember": "#ff8a8a",        # 오류 전용
    "good": "#5fe3a1",         # 잘됨
    "good-bg": "#0e2a1e",
    "warn": "#f2b45c",         # 주의
    "warn-bg": "#2a2011",
    # 구분용 셋 — 다크에서는 **찬 쪽으로 모은다**(청록 → 연보라 → 난초빛).
    "cat1": "#5eead4", "cat1-bg": "#0b2a29",
    "cat2": "#a9c4ff", "cat2-bg": "#141a33",
    "cat3": "#e8b4f8", "cat3-bg": "#241432",
    # 채움(선택된 것) — 다크에서는 **빛나는 유리 알약**이 지금 있는 곳을 말한다.
    "fill-bg": "#d8ecf8",
    "fill-fg": "#05060f",
    "fill-bg-2": "#c7d3ea",    # 진한 배지
    # ── 동작색 — **주 버튼에만** 칠하는 유일한 채도 있는 색 ──────────────────────────
    # 이 색은 **글자로 쓰지 않는다**(어두운 바탕 위 보라는 안 읽힌다) → 아래 INKS에 없고,
    # 대신 「이 바탕 위 흰 글자가 읽히는가」만 잰다.
    "action": "#663af3",
    "action-fg": "#ffffff",
}
# ── 높이(elevation) — **안쪽 서리 + 바깥 후광**으로 만든다 ──────────────────────────
# 종전에는 밑으로 떨어지는 보통 그림자였다. 유리판은 그렇게 뜨지 않는다 : 윗변에 빛이
# 한 줄 걸리고(inset 1px), 판 안쪽으로 넓은 빛이 번지고(inset 24/48px), 판 아래로
# 차가운 어둠이 깔린다. 그 셋을 겹쳐야 「빛을 통과시키는 판」으로 보인다.
# `pop`은 팝업·토스트처럼 **더 떠 있는 것**이다 — 같은 언어에서 아래 어둠만 깊다.
SHADOW = {
    "light": ("rgba(23,23,23,.05) 0 0 0 1px inset,rgba(255,255,255,.7) 0 1px 1px 0 inset,"
              "rgba(63,73,89,.10) 0 12px 24px -12px"),
    "dark": ("rgba(199,211,234,.12) 0 1px 1px 0 inset,"
             "rgba(199,211,234,.05) 0 24px 48px 0 inset,"
             "rgba(6,6,14,.7) 0 24px 32px 0"),
}
SHADOW_POP = {
    "light": ("rgba(23,23,23,.06) 0 0 0 1px inset,rgba(63,73,89,.18) 0 16px 32px -8px"),
    "dark": ("rgba(216,236,248,.2) 0 1px 1px 0 inset,"
             "rgba(168,216,245,.06) 0 24px 48px 0 inset,"
             "rgba(0,0,0,.5) 0 16px 32px 0"),
}
# 워드마크 뒤에 까는 후광 — 글자가 유리 뒤에서 켜진 것처럼 보이게 하는 유일한 장치다.
GLOW = {
    "light": "none",
    "dark": "rgba(186,207,247,.32) 0 0 6px,rgba(186,207,247,.18) 0 0 16px",
}
# ── 바탕 분위기 — 청사진 격자 + 화면 위 후광 ───────────────────────────────────────
# ⚠️ **여기에 둔다.** 화면 코드(`erp_web.py`)에는 색을 직접 적지 않는 것이 이 저장소의 규칙이고
# (`ui_lint`가 hex를 잡는다), 그라데이션은 색 값이 여러 개라 토큰 하나로 넘기지 않으면
# 그 규칙을 어길 수밖에 없다. 화면은 `var(--bg-atmos)` 한 줄만 쓴다.
# 세기 : 선은 푸른 흰빛 4.5%다. 진하면 표 괘선과 다투고, 옅으면 안 보인다.
ATMOS = {
    "light": {"image": "none", "size": "auto"},
    "dark": {
        "image": ("radial-gradient(120% 80% at 50% -10%,rgba(124,145,182,.18) 0%,"
                  "rgba(124,145,182,0) 60%),"
                  "linear-gradient(rgba(186,215,247,.045) 1px,transparent 1px),"
                  "linear-gradient(90deg,rgba(186,215,247,.045) 1px,transparent 1px)"),
        "size": "100% 720px,96px 96px,96px 96px",
    },
}
# 워드마크를 채우는 하늘빛 세로 그라데이션. **가장 큰 글자에만** 쓴다(본문·버튼에 쓰지 않는다).
# 라이트에서는 회사 남색 한 색 그대로다 — 흰 종이 위의 옅은 하늘빛은 안 읽힌다.
WORDMARK = {
    "light": "none",
    "dark": "linear-gradient(0deg,#98c0ef 0%,#d8ecf8 100%)",
}

# 글자를 올리는 배경들. 이 세 면 위에서 다 읽혀야 한다(카드 안 보조면을 빼먹으면
# 「카드에서는 되는데 시트에서는 안 되는」 색이 생긴다).
SURFACES = ("canvas", "paper", "surface-alt")
# 본문 글자 역할 — 큰 글자 예외를 두지 않고 **작은 글자 기준(4.5)**으로 잰다.
# 흐린 글자가 12px 캡션에 쓰이기 때문이다.
INKS = ("ink", "ink-soft", "mid", "ember", "good", "warn", "brand",
        "cat1", "cat2", "cat3")
# ── 자리마다 색이 다르다 (2026-08-06 "로고 색이 메인 색" → 2026-08-09 동작색 분리) ─────
# 세 자리가 **하는 일이 다르다** — 섞어 쓰면 셋 다 뜻을 잃는다.
#   action 보라 = **하는 것**(주 버튼 하나뿐. 화면에서 채도 있는 색은 이것뿐이다)
#   fill        = **있는 곳**(지금 보고 있는 탭 · 지금 고른 필터. 라이트는 검정, 다크는 유리 알약)
#   brand 남색  = **회사**(워드마크 · 링크 · 표의 주인공 열)
# 종전에는 남색이 주 버튼과 링크를 겸했다. 겸하면 「하는 것」과 「회사」가 같은 색이 되고,
# 무엇보다 어두운 바탕에서 남색 글자가 안 읽혀 대비 검사를 통과할 수 없다.
# ── 뜻이 정해진 색 세 가지 (한준 2026-08-06 "포인트 컬러 2개 추가해") ──────────────
# 이 화면은 오래 무채색만 썼다. 읽기는 편했지만 「지금 이게 잘된 건가 아닌가」를 글자를 읽어야
# 알았다. 그래서 **뜻이 있는 자리에만** 색 두 개를 더한다 — 장식으로 쓰지 않는다.
#   ember 빨강 = 오류(사람이 뭔가 해야 한다) · good 초록 = 잘됨 · warn 주황 = 주의
# 「약하게」가 원칙이다(한준) : 배지는 **옅은 바탕 + 같은 계열 진한 글자 + 1px 선**이고,
# 넓은 면을 원색으로 칠하지 않는다. 아래 검사가 세 색 모두 세 표면 위에서 4.5를 넘는지,
# 그리고 **자기 옅은 바탕 위에서도** 넘는지 잰다.
SOFT_PAIRS = (("good", "good-bg"), ("warn", "warn-bg"), ("brand", "brand-bg"),
              ("cat1", "cat1-bg"), ("cat2", "cat2-bg"), ("cat3", "cat3-bg"))

# ── 글자 단계 ────────────────────────────────────────────────────────────────
# 네 단계가 **크기·굵기·색을 함께** 달리해야 구분된다(2026-08-05 한준 "이건 기초잖아").
# 종전에는 넷이 다 12px 대문자 회색이라 「오늘 기록 / 경영 / 이름」이 한 덩어리로 보였다.
TYPE = [
    ("화면 제목", 26, 600, "ink"),
    ("카드 제목", 16, 600, "ink"),
    ("그룹 제목", 13, 600, "ink-soft"),
    ("표 머리", 12, 500, "mid"),
]
# 손가락 최소 크기 — 애플·구글이 함께 권하는 값. **폰에서만** 적용한다(PC는 마우스다).
TAP_MIN = 44
# ── 모서리는 **네 계열뿐**이고 종류마다 하나로 정해져 있다 ─────────────────────────
#   int  999 : 누르는 것 전부 — 버튼·칩·탭·그림 단추. 정사각이면 그대로 동그라미가 된다.
#   card  16 : 카드와 팝업.
#   nested 10 : 카드 **안**에 놓는 판(보조면·표 묶음).
#   sm     6 : 배지와 입력칸.
# ⚠️ 같은 종류에 두 계열을 섞지 않는다 — 그것 하나로 화면이 흐트러진다.
RADIUS = {"int": 999, "card": 16, "nested": 10, "sm": 6}
# 요소 사이 간격 — 기준 4px의 배수만 쓴다(카드 안쪽 24 · 요소 사이 16 · 카드 사이 16).
SPACE = {"card-pad": 24, "gap": 16, "gap-s": 12, "gap-xs": 8}


def _lum(h):
    c = [int(h.lstrip("#")[i:i + 2], 16) / 255 for i in (0, 2, 4)]
    a = [v / 12.92 if v <= 0.03928 else ((v + 0.055) / 1.055) ** 2.4 for v in c]
    return 0.2126 * a[0] + 0.7152 * a[1] + 0.0722 * a[2]


def contrast(fg, bg):
    """두 색의 대비(1~21). 4.5 이상이면 작은 글자도 읽힌다(WCAG AA)."""
    a, b = _lum(fg), _lum(bg)
    return round((max(a, b) + 0.05) / (min(a, b) + 0.05), 2)


def _vars(pal, key):
    out = ["--%s:%s" % (k, v) for k, v in pal.items()]
    out.append("--shadow:" + SHADOW[key])
    out.append("--shadow-pop:" + SHADOW_POP[key])
    out.append("--glow:" + GLOW[key])
    out.append("--bg-atmos:" + ATMOS[key]["image"])
    out.append("--bg-atmos-size:" + ATMOS[key]["size"])
    out.append("--wordmark:" + WORDMARK[key])
    return ";".join(out) + ";"


def css():
    """`<style>` 맨 앞에 넣을 기준값 묶음. `erp_web.py`가 이걸 끼워 쓴다."""
    r = ";".join("--r-%s:%dpx" % (k, v) for k, v in RADIUS.items())
    s = ";".join("--s-%s:%dpx" % (k, v) for k, v in SPACE.items())
    return (
        "/* 기준값 — `ops/ui_tokens.py`가 만든다. **여기를 직접 고치지 말 것**(다음 배포에\n"
        "   덮인다). 색·글자 크기·손가락 크기는 그 파일에서 고치고 `--selftest`로 대비를 확인한다. */\n"
        ":root,[data-theme=light]{color-scheme:light;" + _vars(LIGHT, "light") + "}\n"
        "[data-theme=dark]{color-scheme:dark;" + _vars(DARK, "dark") + "}\n"
        ":root{" + r + ";" + s + ";--tap:%dpx}\n" % TAP_MIN
    )


def selftest():
    """색 대비를 **직접 계산**해서 기준에 못 미치면 실패한다.

    이 검사가 있어서 「색을 조금 예쁘게」 고치다 읽기 어려워지는 일이 막힌다.
    """
    fails, ran = [], [0]

    def ok(name, got, want):
        ran[0] += 1
        if not got:
            fails.append("%s (%s)" % (name, want))

    for theme, pal in (("라이트", LIGHT), ("다크", DARK)):
        for ink in INKS:
            for sur in SURFACES:
                c = contrast(pal[ink], pal[sur])
                ok("%s : %s 를 %s 위에" % (theme, ink, sur), c >= 4.5, "대비 %.2f" % c)
        # 채움(선택된 것·주 버튼)도 읽혀야 한다 — 여기가 낮으면 「고른 것이 안 보인다」가 된다
        for bg in ("fill-bg", "fill-bg-2"):
            c = contrast(pal["fill-fg"], pal[bg])
            ok("%s : 채움 글자를 %s 위에" % (theme, bg), c >= 4.5, "대비 %.2f" % c)
        # 주 버튼은 **동작색**으로 칠한다 — 그 위 글자도 읽혀야 한다.
        # ⚠️ 동작색 자체는 INKS에 없다(글자로 쓰지 않는 색이다) → 여기서만 잰다.
        c = contrast(pal["action-fg"], pal["action"])
        ok("%s : 동작색 위 글자" % theme, c >= 4.5, "대비 %.2f" % c)
        # 뜻이 있는 색은 **자기 옅은 바탕 위에서도** 읽혀야 한다(배지가 그렇게 생겼다)
        for ink, bg in SOFT_PAIRS:
            c = contrast(pal[ink], pal[bg])
            ok("%s : %s 글자를 %s 위에" % (theme, ink, bg), c >= 4.5, "대비 %.2f" % c)
        # 실선은 **글자색이 아니다** — 글자로 쓰면 안 보인다(실측 1.16). 그걸 못 박는다
        c = contrast(pal["hairline"], pal["canvas"])
        ok("%s : 실선색은 글자로 쓸 수 없다(그 사실을 고정)" % theme, c < 3.0,
           "대비 %.2f 라서 글자로 쓸 수 있게 보인다" % c)

    # 글자 단계는 위에서 아래로 **1px 이상** 줄어들어야 한다(같으면 구분이 안 된다)
    for i in range(1, len(TYPE)):
        a, b = TYPE[i - 1], TYPE[i]
        ok("글자 단계 : %s(%dpx) 다음이 %s(%dpx)" % (a[0], a[1], b[0], b[1]),
           a[1] - b[1] >= 1, "차이 %dpx" % (a[1] - b[1]))
    ok("손가락 최소 크기는 44px 이상", TAP_MIN >= 44, "지금 %d" % TAP_MIN)
    # 모서리는 **넷뿐**이고 종류마다 하나다 — 계열이 늘면 「같은 것이 화면마다 다르다」가
    # 모서리에서 다시 시작된다(이 파일을 만든 까닭 그 자체다).
    ok("모서리 계열은 넷", set(RADIUS) == {"int", "card", "nested", "sm"},
       "지금 %s" % sorted(RADIUS))
    ok("누르는 것은 알약이다", RADIUS["int"] >= 999, "지금 %d" % RADIUS["int"])
    ok("카드 > 카드 안 판 > 배지 순으로 모서리가 작아진다",
       RADIUS["card"] > RADIUS["nested"] > RADIUS["sm"],
       "지금 %d/%d/%d" % (RADIUS["card"], RADIUS["nested"], RADIUS["sm"]))
    # 간격은 4px 배수다 — 홀수·자투리 값이 섞이면 줄이 미세하게 어긋나 보인다.
    for k, v in SPACE.items():
        ok("간격 %s(%dpx)는 4의 배수" % (k, v), v % 4 == 0, "4로 안 나뉜다")
    # 만들어진 CSS에 이름이 다 들어갔나(하나라도 빠지면 그 색을 쓰는 곳이 통째로 깨진다)
    made = css()
    for k in LIGHT:
        ok("CSS에 --%s 가 있다" % k, "--%s:" % k in made, "빠졌다")

    print(("selftest 실패 " + " | ".join(fails)) if fails
          else "selftest OK — %d건 통과(대비 %d · 글자 단계 %d · 이름 %d)"
               % (ran[0], 2 * (len(INKS) * len(SURFACES) + len(SOFT_PAIRS) + 4),
                  len(TYPE) - 1, len(LIGHT)))
    return 1 if fails else 0


def main():
    if "--selftest" in sys.argv:
        return selftest()
    print(css())
    print("── 대비 표 ──")
    for theme, pal in (("라이트", LIGHT), ("다크", DARK)):
        for ink in INKS:
            row = "  %-4s %-9s" % (theme, ink)
            for sur in SURFACES:
                row += " %s %.2f" % (sur, contrast(pal[ink], pal[sur]))
            print(row)
    return 0


if __name__ == "__main__":
    sys.exit(main())
