# 처음 한 번은 이 순서로

이 키트를 처음 쓰는 직원을 위한 한 쪽짜리 안내입니다. 여기까지 마치면 첫 사업을 시작할 수 있습니다.

## 1. 읽는 순서

먼저 이 파일(START)을 읽고, 다음 순서로 넘어갑니다.

1. `START.md` — 지금 읽는 이 파일. 설치·점검·첫 사업 흐름.
2. `skills/vax-proposal/SKILL.md` — Claude가 따르는 절차 P0~P6. 사람도 한 번은 읽어 두면 게이트가 왜 있는지 압니다.
3. `skills/vax-proposal/references/writing.md` — 글의 세 축(웹 조사·색채·문장).
4. `skills/vax-proposal/references/deck.md` — 슬라이드 규칙(고스트 덱·제목 사슬).
5. `skills/vax-proposal/references/layouts.md` — 회사 손 덱에서 뽑은 화면 틀 열네 종.

## 2. 설치와 점검

**가장 쉬운 길: 저장소를 내려받은 폴더를 Claude Code로 열고 아래 문장을 그대로 붙여 넣습니다.**

> 이 폴더는 vax-proposal-kit입니다. `bash install.sh`로 설치하고, 스킬의 준비 점검(P-1)을 돌려 빠진 것을 알려 주세요. 제가 해야 하는 것은 한 번에 묶어 주세요.

윈도우는 Git for Windows(Git Bash)가 있어야 `bash`가 됩니다. 없으면 Claude에게 「Git for Windows 설치 안내해 줘」라고 하면 됩니다. 이미 설치가 끝났다면 Claude에게 「준비 점검」이라고만 말합니다. 스킬의 P-1 절차가 `scripts/preflight.py`를 돌리고 커넥터를 확인해, 빠진 것을 번호 목록으로 요구합니다. Claude가 대신 할 수 있는 것(설치·의존성·글꼴)은 「제가 돌릴까요?」라고 묻고 확인 뒤 실행합니다. 아래 두 줄은 손으로 할 때의 길입니다.


저장소를 내려받은 폴더에서 아래 두 줄을 돌립니다.

```bash
bash install.sh     # 스킬 → ~/.claude/skills · 글꼴 → 사용자 글꼴 폴더 · 커밋 훅 연결
bash doctor.sh      # 스킬·글꼴·파이썬·렌더러·판·커밋 훅·웹 검색·저장소 점검
```

`doctor.sh`가 ✗로 짚는 항목을 먼저 처리합니다. 윈도우에서 한글이 깨지던 문제는 스크립트가 안에서 막으므로, 명령 앞에 `PYTHONIOENCODING`을 붙이지 않아도 됩니다.

## 3. 첫 사업은 「접수」로 시작한다
Claude에게 「새 사업 접수」 또는 「공고번호 + 제안」이라고 말하면, 키트가 **RFP 원문 → 노션·피그마 연결 → 주요 방향(당신이 말로) → 재료(이전 제안서·담당자 메모·사진) → 확인**을 한 묶음씩 묻습니다. 다 받아야 초안을 쓰고, 심사 80점·재료점검·승인·피그마 연결이 갖춰져야 피그마를 엽니다. 「그냥 진행해」라고 하면 RFP·피그마 연결·승인 외에는 건너뛰기로 적고 진행합니다.

## 4. 커넥터 점검 — 무엇이 열려야 정상인가

Claude 안에서 아래 둘을 확인합니다. 셸에서는 볼 수 없어 Claude에 직접 물어봐야 합니다.

- **노션**: 재료 팩은 열쇠가 둘입니다. 일반 열쇠로는 기밀 절이 열리지 않으므로, 회사 커넥터로 붙어 있어야 합니다. Claude에 「재료 팩 열어줘」라고 했을 때 공고 밑의 「📦 제안 재료 팩」이 기밀 절까지 다 보이면 정상입니다.
- **피그마**: 회사 계정(vaxport)으로 붙어야 회사 슬라이드 파일이 열립니다. Claude에 「Figma whoami」라고 물어 회사 계정으로 나오면 정상이고, 개인 계정이면 슬라이드 파일 권한이 없습니다.

커넥터는 데스크톱 Claude의 설정 > 커넥터(claude.ai connectors)에서 붙입니다. 노션은 회사 워크스페이스, 피그마는 회사 계정으로 로그인해야 합니다. **둘 중 하나라도 안 열리면** 운영자(한준)에게 슬랙으로 묻거나, 아래 「문제가 생기면」을 따릅니다.

## 5. 팀 공유 점검 — 링크 권한 (사람이 확인)

문서에 걸린 Figma 링크(레이아웃 라이브러리·회사 손 덱 세 개)는 개인 파일에 있으면 다른 직원이 열지 못합니다. **팀 프로젝트로 옮긴 뒤 링크를 갱신**해야 모두가 볼 수 있습니다. 처음 세팅할 때 한 번 확인하고, 안 열리는 링크가 있으면 담당자에게 알립니다.

## 6. 첫 사업 30분 코스

1. Claude에게 「준비 점검」 — 설치·연결에서 빠진 것을 한 번에 알려 주고, 할 수 있는 것은 대신 합니다 (5분)
2. 「새 사업 접수」 또는 「(공고번호) 제안 진행해줘」 — RFP 원문, 노션·피그마 연결, 당신이 생각하는 주요 방향, 이전 제안서·담당자 메모·사진 위치를 차례로 묻습니다. 「없음」도 답입니다 (10분)
3. 재료 팩을 읽고 평가표를 검산하고 가점 게이트를 넘습니다(P0~P2). 여기서 막히면 그 이유를 알려 줍니다 (5분)
4. 발주처 웹 조사와 색채 카드로 방향 후보 셋을 내고 동의를 묻습니다(P3~P3.5). 동의하면 초안 게이트를 확인하고 초안에 들어갑니다 (10분)

여기까지가 첫 30분입니다. 초안(P4)부터는 사업마다 시간이 다르고, 덱은 심사 80점·재료점검·승인·피그마 연결이 갖춰진 뒤에만 시작합니다.

## 문제가 생기면

- 저장소에 이슈를 올립니다: `.github/ISSUE_TEMPLATE/`의 「버그」 또는 「개선 요청」 서식.
- 급하면 운영자(한준)에게 슬랙으로 알립니다. 회사 사실·실명·공고번호·금액은 이슈에 적지 않습니다(저장소가 공개 범위 검사를 합니다).
